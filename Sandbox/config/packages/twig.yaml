twig:
    file_name_pattern: '*.*.twig'
    default_path: '%kernel.project_dir%/Sandbox/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    paths:
        'templates/': ~
        

when@test:
    twig:
        strict_variables: true
